### **完整流程**

1. **创建交易对象：**
   - 定义交易逻辑，包括 zkApp 方法调用（这里是 `update(Field(9))`）。

2. **生成零知识证明：**
   - 验证交易逻辑的合法性，确保状态更新是符合 zkApp 的规则的。

3. **数字签名：**
   - 确保交易的发起方是账户的合法拥有者。

4. **广播交易：**
   - 将交易发送到 Mina 网络，供验证者验证并执行。


---

### **关键点总结**

1. **`Mina.transaction`：**
   - 定义交易逻辑并生成交易对象。

2. **`prove()`：**
   - 生成零知识证明，确保交易逻辑的合法性。

3. **`sign()`：**
   - 对交易进行数字签名，确保发起者的身份和不可篡改性。

4. **`send()`：**
   - 将交易广播到 Mina 网络，触发状态变更。
